import React from 'react';
import { classicLogo } from '../../assets';

const Header = (props) => {
    return (
        <header>
            <img src={classicLogo} />
        </header>
    );
};

export default Header;